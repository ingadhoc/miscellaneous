<odoo>

    <template id="message_activity_done" inherit_id="mail.message_activity_done">
        <span t-field="activity.user_id.name" position="attributes">
            <attribute name="t-field">activity.env.user.name</attribute>
        </span>
    </template>

</odoo>
